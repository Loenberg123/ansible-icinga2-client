nginx:
  - name: nginx-http
    command:
       check_http -I {{ ansible_default_ipv4.address }} -p {{ nginx_ports.stdout }}
  - name: nginx-status
    command:
       check_nginx.sh -H {{ ansible_default_ipv4.address }} -s /status
    plugin:
       check_nginx.sh
